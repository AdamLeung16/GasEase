import type * as Chains from '../../../src/core/Chains.js';
import type * as Mode from '../../../src/core/Mode.js';
import type { Config, Store } from '../../../src/core/Porto.js';
export type Internal<chains extends readonly [Chains.Chain, ...Chains.Chain[]] = readonly [
    Chains.Chain,
    ...Chains.Chain[]
]> = {
    config: Config<chains>;
    id: string;
    getMode: () => Mode.Mode;
    setMode: (i: Mode.Mode) => void;
    store: Store<chains>;
};
//# sourceMappingURL=porto.d.ts.map