import { standardErrorCodes } from 'cbw-sdk/dist/core/error/constants';
import { standardErrors } from 'cbw-sdk/dist/core/error/errors';
import { SerializedError, serializeError } from 'cbw-sdk/dist/core/error/serialize';
import { getErrorCode, getMessageFromCode } from 'cbw-sdk/dist/core/error/utils';
export { getErrorCode, getMessageFromCode, serializeError, standardErrorCodes, standardErrors };
export type ErrorType = Error | SerializedError;
export type ErrorHandler = (error?: ErrorType) => void;
